import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    title: {
        fontSize: 35,
        fontWeight: "bold",
        color: "#000",
    },
    globalMargin: {
        marginHorizontal: 20 //es el margin de isquierda y derecha
    }
});